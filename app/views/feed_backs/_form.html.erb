<%= form_for(@feed_back) do |f| %>
<h3 class="form_title"><%= @title%></h3>
<div class="field">
	<%= f.label "意见",:class=>"input_label" %>
	<div class="input_area">
		<p class="input">
			<%= f.text_area :content,:class=>"basic_input" %><%= mark_required(@feed_back, :content) %>
		</p>
		<p class="tip"></p>
	</div>
	<span class="error_message"></span>    
  </div>
  <div class="actions">
    <%= f.submit "保存" %>
  </div>
<% end %>