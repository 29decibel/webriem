.form_title
  %h3
    = link_to image_tag("/images/icons/add.png"), new_currency_path,:remote=>true
    币种列表
.edit_inline.new_currency
.list_info
  -if currencies.count>0
    = render currencies
  -else
    .empty_info="没有需要付款的单据"
