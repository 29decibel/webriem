<table class="hor-zebra">
  <tr>
    <th></th>
    <th>角色名称</th>
    <th>对应菜单</th>
    <th></th>
  </tr>

<% roles.each do |role| %>
  <tr id="role_<%= role.id%>" class="<%= "odd" if role.id%2==0 %>">
    <%= render "tr_role",:role=>role %>
  </tr>
  <!--this tr is for edit-->
    <tr>
      <td colspan="4">
        <div id="edit_role_<%= role.id%>" class="edit_inline"></div>
      </td>
    </tr>
<% end %>
</table>
