.form_title
  %h3
    = link_to image_tag("/images/icons/add.png"), new_subject_path,:remote=>true
    科目列表
.edit_inline.new_subject
.list_info
  -if subjects.all.count>0
    = render subjects
  -else
    .empty_info="还没有任何科目信息，请点击标题左侧\"+\"进行添加."
=paginate subjects,:remote=>true
